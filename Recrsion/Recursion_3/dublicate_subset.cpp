#include<iostream>
#include<string>
#include<vector>
using namespace std;

void storeSubset(string ans,string original, vector<string>v, bool flag){
    if(original == "")
}

int main(){
    string str = "abc"; // sorted order
    vector<string> v;
    storeSubset("", str, v,true);
}